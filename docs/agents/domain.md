# Domain docs

Single-context repo.

- Glossary: `CONTEXT.md` at repo root
- ADRs: `docs/adr/` (create lazily when needed)

Skills must use CONTEXT.md vocabulary and must not re-litigate ADR decisions without marking that intent.
